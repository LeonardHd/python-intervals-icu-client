name: Publish Python package

on:
  push:
    tags:
      - 'v*'

jobs:
  build-and-publish:
    runs-on: ubuntu-latest

    environment: pypi

    permissions:
      id-token: write
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Poetry and build backend
        run: |
          python -m pip install --upgrade pip
          python -m pip install poetry build

      - name: Set version from tag
        # Expect tags like v1.2.3; strip leading 'v' and set project version
        run: |
          TAG="${GITHUB_REF##*/}"
          VERSION="${TAG#v}"
          echo "Using version $VERSION from tag $TAG"
          poetry version "$VERSION"

      - name: Build package
        run: |
          python -m build

      - name: Publish package distributions to PyPI
        if: startsWith(github.ref, 'refs/tags/')
        uses: pypa/gh-action-pypi-publish@release/v1
